[project]
name = "essa-ai"
version = "0.1.0"
description = "Experimental Essence-Substrate-State-Architecture AI system"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
